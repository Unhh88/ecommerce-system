---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/danaco/src/main/java/com/danaco/entity/Role.java
  - backend/danaco/src/main/java/com/danaco/entity/User.java
  - backend/danaco/src/main/java/com/danaco/entity/Product.java
  - backend/danaco/src/main/java/com/danaco/entity/Order.java
  - backend/danaco/src/main/java/com/danaco/entity/OrderItem.java
  - backend/danaco/src/main/java/com/danaco/entity/Payment.java
  - backend/danaco/src/main/java/com/danaco/entity/Delivery.java
  - backend/danaco/src/main/java/com/danaco/entity/UserAddress.java
  - backend/danaco/src/main/java/com/danaco/entity/Notification.java
  - backend/danaco/src/main/java/com/danaco/entity/Complaint.java
  - backend/danaco/src/main/java/com/danaco/entity/RefundRequest.java
  - backend/danaco/src/main/java/com/danaco/entity/Invoice.java
  - backend/danaco/src/main/java/com/danaco/entity/AuditLog.java
  - backend/danaco/src/main/java/com/danaco/entity/StockHistory.java
autonomous: true
requirements:
  - DB-01
  - DB-02

must_haves:
  truths:
    - "All 14 JPA entity classes exist with proper @Entity and @Table annotations"
    - "Entities have correct relationships (@OneToMany, @ManyToOne, @ManyToMany)"
    - "Entities use UUID as primary keys with @GeneratedValue"
    - "Lombok @Data annotation reduces boilerplate"
    - "Database schema matches Flyway migration (V20260408__initial_schema.sql)"
  artifacts:
    - path: "backend/danaco/src/main/java/com/danaco/entity/"
      provides: "All JPA entity classes"
      files: 14
    - path: "backend/danaco/src/main/resources/db/migration/V20260408__initial_schema.sql"
      provides: "PostgreSQL schema (already created)"
      tables: 14
  key_links:
    - from: "Role entity"
      to: "User entity"
      via: "@ManyToOne @JoinColumn"
    - from: "User entity"
      to: "UserAddress entity"
      via: "@OneToMany cascade DELETE"
    - from: "Order entity"
      to: "OrderItem entity"
      via: "@OneToMany cascade DELETE"
    - from: "Order entity"
      to: "Payment entity"
      via: "@OneToOne"
---

<objective>
Create JPA entities for all 14 database tables with proper relationships, annotations, and Lombok integration.

Purpose: Establish type-safe ORM layer that matches the PostgreSQL schema; foundation for all repository and service layers.

Output: 14 entity classes (Role, User, Product, Order, OrderItem, Payment, Delivery, UserAddress, Notification, Complaint, RefundRequest, Invoice, AuditLog, StockHistory) ready for repository creation.
</objective>

<execution_context>
@c:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/danaco/pom.xml (Lombok already configured)
@backend/danaco/src/main/resources/db/migration/V20260408__initial_schema.sql (schema reference)
@CLAUDE.md (entity naming conventions, RBAC role definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Core Entity Classes (Role, User)</name>
  <files>
    backend/danaco/src/main/java/com/danaco/entity/Role.java
    backend/danaco/src/main/java/com/danaco/entity/User.java
  </files>
  <action>
Create @Entity classes for Role and User.

**Role.java:**
- @Id UUID id (generated)
- @Column String name (VARCHAR 50, unique)
- @Column String description (TEXT, nullable)
- @ManyToOne List<User> users (reverse FK)
- Lombok @Data @NoArgsConstructor @AllArgsConstructor
- Enum-backed optional: use string column, not enum type

**User.java:**
- @Id UUID id (generated)
- @Column String email (VARCHAR 255, unique, nullable)
- @Column String phoneNumber (VARCHAR 20, unique, nullable)
- CONSTRAINT: email OR phoneNumber must be NOT NULL
- @Column String username (VARCHAR 100, unique)
- @Column String passwordHash (VARCHAR 255, NOT NULL)
- @Column String firstName, lastName (100 each, nullable)
- @Column Boolean isActive, isBlocked (default true/false)
- @Column Integer failedOrdersCount (default 0) - for 3-cancel rule
- @ManyToOne Role role (required, @JoinColumn "role_id")
- @OneToMany List<UserAddress> addresses (cascade DELETE)
- Lombok @Data @NoArgsConstructor @AllArgsConstructor
- @Table name "user" (reserved keyword, quoted)

**Relationships:**
User.role → Role (many-to-one)
Role.users ← User (one-to-many, mapped by User.role)

**Timestamps:**
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt
  </action>
  <verify>
    <automated>javac -d /tmp backend/danaco/src/main/java/com/danaco/entity/Role.java backend/danaco/src/main/java/com/danaco/entity/User.java 2>&1 | grep -i "error" || echo "✅ Compilation successful"</automated>
  </verify>
  <done>
- Role.java compiles with @Entity, UUID id, name, description
- User.java compiles with @Entity @Table("user"), email/phone/username unique constraints
- User has @ManyToOne Role relationship, addresses @OneToMany
- Both use Lombok @Data, timestamps
- No compilation errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Product & Inventory Entities</name>
  <files>
    backend/danaco/src/main/java/com/danaco/entity/Product.java
    backend/danaco/src/main/java/com/danaco/entity/StockHistory.java
  </files>
  <action>
**Product.java:**
- @Id UUID id
- @Column String name (VARCHAR 255, unique)
- @Column String description (TEXT, nullable)
- @Column String category (VARCHAR 100)
- @Column String imageUrl (VARCHAR 500, nullable)
- @Column BigDecimal price (DECIMAL 10,2)
- @Column Integer stockQuantity (default 0)
- @Column Boolean isActive (default true)
- @ManyToOne User createdBy (required, @JoinColumn "created_by_id")
- @OneToMany List<StockHistory> stockHistories (cascade DELETE)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**StockHistory.java:**
- @Id UUID id
- @ManyToOne Product product (required, @JoinColumn "product_id")
- @Column Integer previousQuantity, newQuantity
- @Column String changeReason (VARCHAR 100, e.g., "ADDED", "SOLD", "ADJUSTED")
- @ManyToOne User changedBy (required, @JoinColumn "changed_by_id")
- @CreationTimestamp LocalDateTime createdAt (no updatedAt for history)

**Relationships:**
Product.stockHistories ← StockHistory (one-to-many, cascade on FK delete)
Product.createdBy → User (many-to-one)
StockHistory.product → Product
StockHistory.changedBy → User
  </action>
  <verify>
    <automated>cd backend/danaco && mvn compile -DskipTests 2>&1 | tail -20 | grep -i "BUILD SUCCESS" || echo "Check compilation output"</automated>
  </verify>
  <done>
- Product.java compiles with @Entity, UUID, name/category/price
- StockHistory.java compiles with @Entity, product and changedBy FKs
- Both entities have timestamp annotations
- No compilation errors when mvn compile runs
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Order & OrderItem Entities</name>
  <files>
    backend/danaco/src/main/java/com/danaco/entity/Order.java
    backend/danaco/src/main/java/com/danaco/entity/OrderItem.java
  </files>
  <action>
**Order.java:**
- @Id UUID id
- @ManyToOne User customer (required, @JoinColumn "customer_id")
- @Column String orderNumber (VARCHAR 50, unique)
- @Column String status (VARCHAR 50, default "PENDING") - values: PENDING, APPROVED, PROCESSING, SHIPPED, DELIVERED, CANCELLED
- @Column BigDecimal totalAmount (DECIMAL 10,2)
- @Column String paymentMethod (VARCHAR 50, nullable) - CREDIT_CARD, CASH, etc.
- @ManyToOne UserAddress billingAddress (nullable, @JoinColumn)
- @ManyToOne UserAddress shippingAddress (nullable, @JoinColumn)
- @Column String trackingNumber (VARCHAR 100, nullable)
- @Column String notes (TEXT, nullable)
- @OneToMany List<OrderItem> items (cascade DELETE, cascade PERSIST)
- @Column LocalDateTime approvedAt, shippedAt, deliveredAt (all nullable)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**OrderItem.java:**
- @Id UUID id
- @ManyToOne Order order (required, @JoinColumn "order_id")
- @ManyToOne Product product (required, @JoinColumn "product_id")
- @Column Integer quantity (NOT NULL)
- @Column BigDecimal unitPrice (DECIMAL 10,2)
- @Column BigDecimal subtotal (DECIMAL 10,2) - should equal quantity * unitPrice
- @CreationTimestamp LocalDateTime createdAt

**Relationships:**
Order.items ← OrderItem (one-to-many, cascade DELETE + PERSIST)
OrderItem.order → Order (many-to-one)
OrderItem.product → Product (many-to-one, not bidirectional)
Order.customer → User
Order.billingAddress → UserAddress
Order.shippingAddress → UserAddress
  </action>
  <verify>
    <automated>cd backend/danaco && mvn compile -DskipTests 2>&1 | grep -E "(BUILD SUCCESS|ERROR)" | head -1</automated>
  </verify>
  <done>
- Order.java compiles with status enum options, customer/items relationships
- OrderItem.java compiles with order/product FKs and price fields
- Cascade rules set correctly (DELETE, PERSIST)
- mvn compile succeeds
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Payment, Delivery, Address Entities</name>
  <files>
    backend/danaco/src/main/java/com/danaco/entity/Payment.java
    backend/danaco/src/main/java/com/danaco/entity/UserAddress.java
    backend/danaco/src/main/java/com/danaco/entity/Delivery.java
  </files>
  <action>
**Payment.java:**
- @Id UUID id
- @OneToOne Order order (required, @JoinColumn "order_id")
- @Column BigDecimal amount (DECIMAL 10,2)
- @Column String paymentMethod (VARCHAR 50, NOT NULL)
- @Column String status (VARCHAR 50, default "PENDING") - PENDING, COMPLETED, FAILED, REFUNDED
- @Column String transactionId (VARCHAR 255, nullable)
- @CreationTimestamp LocalDateTime createdAt
- @Column LocalDateTime processedAt (nullable)

**UserAddress.java:**
- @Id UUID id
- @ManyToOne User user (required, @JoinColumn "user_id")
- @Column String addressLine1 (VARCHAR 255, NOT NULL)
- @Column String addressLine2 (VARCHAR 255, nullable)
- @Column String city (VARCHAR 100, NOT NULL)
- @Column String stateProvince (VARCHAR 100, nullable)
- @Column String postalCode (VARCHAR 20, nullable)
- @Column String country (VARCHAR 100, NOT NULL)
- @Column String phoneNumber (VARCHAR 20, nullable)
- @Column Boolean isDefault (default false)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**Delivery.java:**
- @Id UUID id
- @OneToOne Order order (required, @JoinColumn "order_id")
- @ManyToOne User deliveryStaff (nullable, @JoinColumn "delivery_staff_id")
- @Column String status (VARCHAR 50, default "PENDING") - PENDING, OUT_FOR_DELIVERY, DELIVERED, FAILED_DELIVERY, ORDER_PICKED_UP
- @ManyToOne UserAddress deliveryAddress (nullable, @JoinColumn)
- @Column LocalDate estimatedDeliveryDate, actualDeliveryDate (both nullable)
- @Column String deliveryNotes (TEXT, nullable)
- @Column String proofPhotoUrl (VARCHAR 500, nullable)
- @Column String customerSignatureUrl (VARCHAR 500, nullable)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**Relationships:**
Payment.order → Order (one-to-one, FK in Payment table)
UserAddress.user → User (many-to-one, cascade delete)
Delivery.order → Order (one-to-one, FK in Delivery table)
Delivery.deliveryStaff → User (many-to-one, nullable)
Delivery.deliveryAddress → UserAddress

**Note:** User.addresses is already @OneToMany to UserAddress from Task 1
  </action>
  <verify>
    <automated>cd backend/danaco && mvn compile -DskipTests 2>&1 | grep -E "(BUILD SUCCESS|error:)" | head -1</automated>
  </verify>
  <done>
- Payment.java compiles with @OneToOne Order, status, transactionId
- UserAddress.java compiles with user FK, address fields, isDefault flag
- Delivery.java compiles with order, deliveryStaff, deliveryAddress FKs
- All three compile successfully
  </done>
</task>

<task type="auto">
  <name>Task 5: Create Notification, Complaint, Refund, Invoice, AuditLog Entities</name>
  <files>
    backend/danaco/src/main/java/com/danaco/entity/Notification.java
    backend/danaco/src/main/java/com/danaco/entity/Complaint.java
    backend/danaco/src/main/java/com/danaco/entity/RefundRequest.java
    backend/danaco/src/main/java/com/danaco/entity/Invoice.java
    backend/danaco/src/main/java/com/danaco/entity/AuditLog.java
  </files>
  <action>
**Notification.java:**
- @Id UUID id
- @ManyToOne User recipient (required, @JoinColumn "recipient_id", cascade DELETE)
- @Column String type (VARCHAR 50, NOT NULL) - STOCK_LOW, ORDER_UPDATE, etc.
- @Column String title (VARCHAR 255)
- @Column String message (TEXT NOT NULL)
- @Column UUID relatedOrderId (nullable) - NOT a FK, just a reference
- @Column UUID relatedProductId (nullable) - NOT a FK, just a reference
- @Column Boolean isRead (default false)
- @CreationTimestamp LocalDateTime createdAt

**Complaint.java:**
- @Id UUID id
- @ManyToOne Order order (required, @JoinColumn "order_id")
- @ManyToOne User customer (required, @JoinColumn "customer_id")
- @Column String description (TEXT NOT NULL)
- @Column String status (VARCHAR 50, default "OPEN") - OPEN, IN_PROGRESS, RESOLVED, CLOSED
- @Column String response (TEXT, nullable)
- @ManyToOne User assignedTo (nullable, @JoinColumn)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**RefundRequest.java:**
- @Id UUID id
- @ManyToOne Order order (required, @JoinColumn "order_id")
- @ManyToOne User customer (required, @JoinColumn "customer_id")
- @Column BigDecimal amount (DECIMAL 10,2)
- @Column String reason (TEXT NOT NULL)
- @Column String status (VARCHAR 50, default "PENDING") - PENDING, APPROVED, REJECTED, PROCESSED
- @ManyToOne User approvedBy (nullable, @JoinColumn)
- @Column LocalDateTime processedAt (nullable)
- @CreationTimestamp LocalDateTime createdAt
- @UpdateTimestamp LocalDateTime updatedAt

**Invoice.java:**
- @Id UUID id
- @OneToOne Order order (required, @JoinColumn "order_id")
- @Column String invoiceNumber (VARCHAR 50, unique)
- @Column String pdfUrl (VARCHAR 500, nullable)
- @Column BigDecimal totalAmount (DECIMAL 10,2)
- @CreationTimestamp LocalDateTime issuedAt
- @Column LocalDate dueDate (nullable)

**AuditLog.java:**
- @Id UUID id
- @Column String action (VARCHAR 100, NOT NULL)
- @Column String entityType (VARCHAR 50, NOT NULL)
- @Column UUID entityId (nullable)
- @ManyToOne User user (nullable, @JoinColumn "user_id")
- @Column String changes (using @Column with type="jsonb" for PostgreSQL JSONB) - alternatively: String (TEXT) if JSONB not needed yet
- @CreationTimestamp LocalDateTime createdAt (no updatedAt for audit)

**Relationships:**
Notification.recipient → User (cascade DELETE)
Complaint.order → Order (many-to-one)
Complaint.customer → User (many-to-one)
Complaint.assignedTo → User (optional, many-to-one)
RefundRequest.order → Order
RefundRequest.customer → User
RefundRequest.approvedBy → User (optional)
Invoice.order → Order (one-to-one)
AuditLog.user → User (optional)
  </action>
  <verify>
    <automated>cd backend/danaco && mvn clean compile -DskipTests 2>&1 | tail -3 | grep "BUILD SUCCESS" || echo "Build check"</automated>
  </verify>
  <done>
- All 5 entity classes compile successfully
- Notification.java created with recipient FK
- Complaint.java created with order, customer, assignedTo FKs
- RefundRequest.java created with order, customer, approvedBy FKs
- Invoice.java created with @OneToOne Order
- AuditLog.java created with user FK and changes field
- mvn clean compile succeeds
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database ↔ JPA Layer | Entities represent untrusted data from DB; must validate on insert/update |
| API ↔ Entities | DTO layer will validate/transform data before entity persistence |
| Entities ↔ Services | Services must enforce authorization before entity operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | User.passwordHash | mitigate | Never log or expose password hash; always use bcrypt comparison (Phase 2) |
| T-01-02 | Data Integrity | Order entities | mitigate | Foreign key constraints enforced at DB level; cascade rules prevent orphans |
| T-01-03 | Information Disclosure | AuditLog.changes | mitigate | Don't log sensitive fields (passwords, tokens); sanitize in Phase 2 |
| T-01-04 | Privilege Escalation | User roles in entities | mitigate | RBAC validation at API layer (Phase 2); entities just store role_id |
| T-01-05 | Injection | AuditLog JSONB field | accept | JPA parameterized queries prevent injection; JSONB field is auto-escaped |

</threat_model>

<verification>
All 14 entity classes created and compiling cleanly. No circular dependencies. Relationships match database schema (V20260408__initial_schema.sql). Ready for repository layer.
</verification>

<success_criteria>
- ✅ 14 JPA @Entity classes created in `backend/danaco/src/main/java/com/danaco/entity/`
- ✅ All entities use @Id UUID with @GeneratedValue
- ✅ All relationships (@OneToMany, @ManyToOne, @OneToOne) properly configured
- ✅ Cascade rules set (DELETE on @OneToMany collections)
- ✅ Timestamps (@CreationTimestamp, @UpdateTimestamp) on all entities
- ✅ Lombok @Data, @NoArgsConstructor, @AllArgsConstructor applied
- ✅ Foreign key columns match database schema naming (e.g., user_id, product_id)
- ✅ mvn clean compile succeeds with no errors
- ✅ Ready for Phase 01 Plan 02 (Repository & Service layer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-PLAN-SUMMARY.md`
</output>
